## Rust DS MakmillerServ — конфіг очищено (2026-04)

- **`server.cfg`**: нормалізовано опис; прибрано сміттєвий `server.url` (`////////////`); задано робочий RCON-пароль (≥8 символів); збережено банер pngtree PNG для `server.headerimage`.
- **Чому не було банера**: У **`serverauto.cfg`** було **`server.headerimage ""`** — воно **перезаписувало** значення з `server.cfg`. Рядок оновлено на той самий URL, що й у `server.cfg`.
- Файл **`serverauto.cfg`** сервер інколи перегенерує при зупинці; якщо знову стане `""`, повторити заміну або синхронізувати з `server.cfg`.
- Після змін — **повний рестарт** Rust Dedicated.
