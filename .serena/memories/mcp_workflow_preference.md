## MCP workflow (user preference)

- Use **user-serena** for all edits under project `portprobros` (`D:\Project\myprog\portprobros`): `activate_project` → `read_file` / `replace_content` / symbol tools; `write_memory` for session/project notes.
- **user-filesystem** allowed roots (this session): `D:\Project` only. Paths outside (e.g. `D:\RustServer\Server`) are **not** covered by filesystem MCP unless the allowlist is extended in Cursor MCP settings.
- Always read tool JSON schemas under `mcps/<server>/tools/` before first use.
