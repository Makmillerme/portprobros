# Suggested commands

## VPS (`server/`)

```bash
docker compose up -d
docker compose logs -f duckdns
```

## Windows (`client/`)

- `start.bat`
- `scripts\Setup-WireGuardTunnel.ps1` (Admin PowerShell)

Secrets: не комітити `server/.env`.
