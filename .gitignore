# Secrets
server/.env

# WireGuard secrets — never commit
client/wg0.conf
client/*.key
client/scripts/*.key
client/tools/downloads/

# Serena AI dev-workflow memories (personal, not project docs)
.serena/memories/
